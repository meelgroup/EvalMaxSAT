# Config file for the @EXPORT_TYPE@ EvalMaxSAT Package
# It defines the following variables
#  EVALMAXSAT_INCLUDE_DIRS - include directories for EvalMaxSAT
#  EVALMAXSAT_LIBRARIES    - libraries to link against
#  EVALMAXSAT_EXECUTABLE   - the EvalMaxSAT executable

# Compute paths
get_filename_component(EVALMAXSAT_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(EVALMAXSAT_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${EVALMAXSAT_CMAKE_DIR}/@EVALMAXSAT_TARGETS_FILENAME@")

# These are IMPORTED targets created by @EVALMAXSAT_TARGETS_FILENAME@
set(EVALMAXSAT_LIBRARIES EvalMaxSAT)
set(EVALMAXSAT_STATIC_LIBRARIES EvalMaxSAT)
set(EVALMAXSAT_LIBRARIES_DEPS evalmax_cadical)
set(EVALMAXSAT_VERSION_MAJOR @EVALMAXSAT_VERSION_MAJOR@)
set(EVALMAXSAT_VERSION_MINOR @EVALMAXSAT_VERSION_MINOR@)
set(EVALMAXSAT_VERSION_PATCH @EVALMAXSAT_VERSION_PATCH@)
set(EVALMAXSAT_VERSION @EVALMAXSAT_VERSION@)
set(EVALMAXSAT_EXECUTABLE EvalMaxSAT_bin)
